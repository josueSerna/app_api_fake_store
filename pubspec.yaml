name: package_fake_store
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  fake_store_client:
    git:
      url: https://github.com/josueSerna/fake_store_client.git
  ds_atomic:
    git:
      url: https://github.com/josueSerna/ds_atomic


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true
